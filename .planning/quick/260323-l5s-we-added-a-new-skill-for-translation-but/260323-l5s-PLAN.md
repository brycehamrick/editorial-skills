---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [readme-translation-update]

must_haves:
  truths:
    - "README usage sections mention editorial-translation as an optional step"
    - "README standalone examples include a translation trigger phrase"
    - "README example workflow includes a translation step"
  artifacts:
    - path: "README.md"
      provides: "Complete documentation reflecting editorial-translation skill"
      contains: "/editorial-translation"
  key_links: []
---

<objective>
Update README.md usage and example sections to include editorial-translation.

Purpose: The Available Skills table, workflow diagram, and Skill Reference section already document editorial-translation. But the usage sections (Run the Full Workflow, Or Jump to What You Need, Example Workflow) do not mention it. This makes the README inconsistent -- a reader sees the skill listed but never sees how to use it.

Output: Updated README.md with translation woven into usage examples.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@skills/editorial-translation/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add editorial-translation to README usage and example sections</name>
  <files>README.md</files>
  <action>
Make three targeted additions to README.md. Do NOT touch the Available Skills table, workflow diagram, or Skill Reference section -- those are already correct.

1. **"Run the Full Workflow" section (around lines 161-181):**
   - In the print books workflow block, add `/editorial-translation` as an optional step between `/editorial-copy` and `/editorial-typesetting`, with the comment: `(optional: translate into target language)`. Use a comment or note to indicate it is optional, not required for every project.
   - In the essays/articles workflow block, add `/editorial-translation` as an optional step between `/editorial-copy` and `/editorial-proof`, same optional annotation.

2. **"Or Jump to What You Need" standalone examples (around lines 185-208):**
   Add a translation example entry:
   ```
   "Translate this into Spanish"
   -> Uses editorial-translation
   ```
   Place it after the editorial-copy example and before the editorial-typesetting example.

3. **"Example Workflow" section (around lines 212-239):**
   Add one translation step between the copy edit step and the typesetting step:
   ```
   You: Translate the manuscript into Spanish for the Latin American edition.
   -> /editorial-translation reads the context, translates the full document
   ```

Keep the existing tone, formatting, and style of the README. Do not add explanatory text beyond what is needed.
  </action>
  <verify>
    <automated>grep -c "editorial-translation" README.md</automated>
  </verify>
  <done>The grep count increases from the current baseline (currently appears ~5 times in the skills table, diagram, and reference sections). After this task it should appear at least 8 times, confirming it now shows up in usage and example sections too.</done>
</task>

</tasks>

<verification>
- All three usage sections (full workflow, standalone examples, example workflow) mention editorial-translation
- Translation is marked as optional in the full workflow blocks
- No changes to the Already-correct sections (Available Skills table, workflow diagram, Skill Reference)
- README renders cleanly with no broken formatting
</verification>

<success_criteria>
A reader who scans the README usage examples will see how and when to invoke editorial-translation, consistent with the rest of the documented workflow.
</success_criteria>

<output>
After completion, create `.planning/quick/260323-l5s-we-added-a-new-skill-for-translation-but/260323-l5s-SUMMARY.md`
</output>
