---
phase: 999.1-editorial-translation-skill
plan: 03
type: execute
wave: 3
depends_on: ["999.1-01", "999.1-02"]
files_modified:
  - CLAUDE.md
  - README.md
  - skills/editorial-context/SKILL.md
autonomous: false
requirements:
  - ET-INTEGRATION
  - D-08
  - D-09

must_haves:
  truths:
    - "CLAUDE.md Current State table includes editorial-translation with correct status"
    - "CLAUDE.md Repository Structure includes editorial-translation directory tree"
    - "README.md documents editorial-translation in the skills table and skill reference section"
    - "editorial-context SKILL.md workflow list includes editorial-translation as optional between stages 4 and 5"
    - "Skill is synced to ~/.claude/skills/editorial-translation/"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated project state with editorial-translation"
      contains: "editorial-translation"
    - path: "README.md"
      provides: "Updated documentation with editorial-translation"
      contains: "editorial-translation"
    - path: "skills/editorial-context/SKILL.md"
      provides: "Updated workflow list with optional translation step"
      contains: "editorial-translation"
  key_links:
    - from: "CLAUDE.md"
      to: "skills/editorial-translation/SKILL.md"
      via: "Current State table entry"
      pattern: "editorial-translation"
    - from: "skills/editorial-context/SKILL.md"
      to: "skills/editorial-translation/SKILL.md"
      via: "Workflow position list mentioning optional translation"
      pattern: "editorial-translation"
---

<objective>
Update all integration files to include editorial-translation: CLAUDE.md (current state, structure, conventions), README.md (skills table, reference section), and editorial-context/SKILL.md (workflow list). Then sync the skill to ~/.claude/skills/.

Purpose: The skill must be discoverable and documented across the project. Without these updates, the skill exists but is invisible to the project's orientation files.
Output: Updated CLAUDE.md, README.md, editorial-context/SKILL.md. Skill synced to active skills directory.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/999.1-editorial-translation-skill/999.1-CONTEXT.md
@.planning/phases/999.1-editorial-translation-skill/999.1-01-SUMMARY.md
@CLAUDE.md
@README.md
@skills/editorial-context/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md with editorial-translation skill</name>
  <files>CLAUDE.md</files>
  <read_first>
    CLAUDE.md
    .planning/phases/999.1-editorial-translation-skill/999.1-CONTEXT.md
  </read_first>
  <action>
    Update three sections of CLAUDE.md:

    **1. Current Project State -- Skills built table:**
    Add a new row after `editorial-proof`:
    ```
    | `editorial-translation` | Optional (4-5) | Complete -- adapted from baoyu-translate, single-mode, Spanish default, codex opt-in |
    ```
    The stage column should say `Optional (4-5)` to indicate it sits between stages 4 and 5 as an optional branch (per D-08).

    **2. Repository Structure:**
    Add `editorial-translation` directory tree after `editorial-proof` entry:
    ```
        ├── editorial-translation/
        │   ├── SKILL.md                   # Optional: nonfiction document translation (any language pair)
        │   ├── scripts/
        │   │   ├── main.ts                # CLI entry point for markdown chunking
        │   │   ├── chunk.ts               # Markdown chunking at block boundaries
        │   │   ├── package.json
        │   │   └── bun.lock
        │   └── references/
        │       ├── ANALYSIS-WORKFLOW.md    # Content analysis and translation workflow steps
        │       ├── EXTEND-SCHEMA.md        # EXTEND.md preferences file schema
        │       └── STYLE-PRESETS.md        # Audience and style preset tables
    ```

    **3. Key Design Decisions:**
    Add a new decision entry:
    ```
    **Translation is a production step, not a diagnostic.** `editorial-translation` follows the same shape as `editorial-typesetting` -- it produces an artifact (translated document), not an issue list. It reads `.agents/editorial-context.md` but does not write to it. No T# issue prefix, no diagnostic phase.
    ```

    **4. Established conventions (in Current State):**
    Add to the bullet list:
    - `editorial-translation` is modeled on `editorial-typesetting` (production output, not diagnostic-first)
    - `editorial-translation` has its own chunking scripts (copied from baoyu-translate, self-contained in scripts/)

    Do NOT change any other content in CLAUDE.md.
  </action>
  <verify>
    <automated>grep "editorial-translation" CLAUDE.md && grep "Optional (4-5)" CLAUDE.md && grep "ANALYSIS-WORKFLOW.md" CLAUDE.md && grep "production step, not a diagnostic" CLAUDE.md && echo "CLAUDE.md updated"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains `editorial-translation` in the Skills built table
    - CLAUDE.md contains `Optional (4-5)` as the stage for editorial-translation
    - CLAUDE.md Repository Structure contains `editorial-translation/` directory tree with scripts/ and references/ subdirectories
    - CLAUDE.md Key Design Decisions contains `Translation is a production step, not a diagnostic`
    - CLAUDE.md Established conventions contains `editorial-translation` modeled on `editorial-typesetting`
  </acceptance_criteria>
  <done>CLAUDE.md reflects editorial-translation in current state, structure, conventions, and design decisions</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md and editorial-context/SKILL.md, then sync skill</name>
  <files>
    README.md
    skills/editorial-context/SKILL.md
  </files>
  <read_first>
    README.md
    skills/editorial-context/SKILL.md
    .planning/phases/999.1-editorial-translation-skill/999.1-CONTEXT.md
  </read_first>
  <action>
    **Update README.md:**
    1. Add `editorial-translation` to the skills table (wherever the existing skills are listed). Position it after `editorial-copy` and before `editorial-typesetting` as an optional step. Brief description: "Translation of finished nonfiction documents into any target language (default: Spanish). Optional step between copy editing and typesetting."
    2. Add a skill reference section for editorial-translation (following the pattern of other skill reference entries in the README). Include: what it does, when to use it, default target language (Spanish), codex opt-in, reference files list.
    3. If there is an ASCII workflow diagram, add editorial-translation as an optional branch between stages 4 and 5 (per D-08/D-09 -- do not renumber existing stages).

    **Update editorial-context/SKILL.md:**
    In the `## What Comes Next` section at the bottom, update the workflow list to include editorial-translation as an optional step. The current list ends with stages 1-6 and a note about stages 5-6. Add editorial-translation between stages 4 and 5:

    After the line for `4. **editorial-copy**...` add:
    ```markdown
       - **editorial-translation** -- Optional: translate the copy-edited document into another language *(only when translation is needed)*
    ```
    Use a sub-bullet or indented note (matching the style of the existing "Note on stages 5-6" if possible) to indicate it's optional. Do NOT renumber any existing stages.

    **Sync skill to active directory:**
    Run:
    ```bash
    cp -r "skills/editorial-translation" ~/.claude/skills/
    ```
  </action>
  <verify>
    <automated>grep "editorial-translation" README.md && grep "editorial-translation" skills/editorial-context/SKILL.md && test -d ~/.claude/skills/editorial-translation && test -f ~/.claude/skills/editorial-translation/SKILL.md && echo "Integration complete"</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `editorial-translation` in the skills table
    - README.md contains a skill reference section for editorial-translation
    - `skills/editorial-context/SKILL.md` contains `editorial-translation` in the What Comes Next workflow list
    - `skills/editorial-context/SKILL.md` shows editorial-translation as optional (not a numbered stage)
    - `~/.claude/skills/editorial-translation/SKILL.md` exists (synced)
    - `~/.claude/skills/editorial-translation/references/ANALYSIS-WORKFLOW.md` exists (synced)
    - `~/.claude/skills/editorial-translation/scripts/main.ts` exists (synced)
  </acceptance_criteria>
  <done>README.md and editorial-context updated with editorial-translation. Skill synced to active skills directory.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify editorial-translation skill</name>
  <what-built>
    Complete editorial-translation skill with:
    - SKILL.md (main skill file, under 500 lines)
    - 3 reference files (ANALYSIS-WORKFLOW.md, EXTEND-SCHEMA.md, STYLE-PRESETS.md)
    - Chunking scripts (copied from baoyu-translate)
    - Updated CLAUDE.md, README.md, and editorial-context/SKILL.md
    - Synced to ~/.claude/skills/
  </what-built>
  <how-to-verify>
    1. Read `skills/editorial-translation/SKILL.md` -- check that it reads like a professional editorial skill, follows the production-output pattern (not diagnostic-first), and defaults to Spanish
    2. Confirm codex is opt-in (not default) in the skill
    3. Confirm there are no Chinese characters or baoyu-specific references anywhere in the skill
    4. Confirm the workflow position shows editorial-translation as optional between stages 4 and 5
    5. Check `~/.claude/skills/editorial-translation/` exists with all files
    6. Optionally: test the skill by saying "translate this paragraph to Spanish" to see if it triggers correctly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `grep -c "editorial-translation" CLAUDE.md` returns 4+ matches (table, structure, conventions, design decisions)
2. `grep "editorial-translation" README.md` returns matches in skills table and reference section
3. `grep "editorial-translation" skills/editorial-context/SKILL.md` returns match in workflow list
4. `ls ~/.claude/skills/editorial-translation/` shows SKILL.md, references/, scripts/
5. No Chinese characters anywhere: `grep -rP "[\x{4e00}-\x{9fff}]" skills/editorial-translation/` returns nothing
</verification>

<success_criteria>
- CLAUDE.md, README.md, and editorial-context/SKILL.md all reference editorial-translation correctly
- Skill is synced to ~/.claude/skills/ and ready for use
- Human has verified the complete skill
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-editorial-translation-skill/999.1-03-SUMMARY.md`
</output>
