---
phase: 999.1-editorial-translation-skill
plan: 02
subsystem: skills/editorial-translation
tags: [editorial-translation, skill-file, spanish, codex-integration]
dependency_graph:
  requires:
    - 999.1-01 (chunking scripts and reference files)
  provides:
    - skills/editorial-translation/SKILL.md
  affects:
    - 999.1-03-PLAN.md (CLAUDE.md, README.md, editorial-context, sync)
tech_stack:
  added: []
  patterns:
    - Production-output skill (no diagnostic phase, read-only context access)
    - Blocking first-time EXTEND.md setup via AskUserQuestion
    - Codex opt-in with model override (gpt-5.4-mini)
    - Single-mode workflow (no quick/normal/refined branching)
key_files:
  created:
    - skills/editorial-translation/SKILL.md
  modified: []
decisions:
  - "SKILL.md uses production-skill pattern modeled on editorial-typesetting (read-only context file, no issue log)"
  - "Codex section explicitly states gpt-5.4-mini overrides codex skill auto-selection table with note to check for newer models"
  - "First-time setup is BLOCKING via AskUserQuestion in one call -- no silent defaults per Pitfall 5"
  - "stop-slop referenced in Step 7 translation step for production prose quality"
  - "Translator's notes format adapted from Chinese format to general: [target text] ([source term] -- [brief explanation])"
metrics:
  duration: 3 minutes
  completed: 2026-03-23
  tasks_completed: 1
  files_created: 1
---

# Phase 999.1 Plan 02: Write SKILL.md for editorial-translation -- Summary

**One-liner:** Complete SKILL.md implementing all 13 locked decisions with production-skill pattern, Spanish defaults, codex opt-in, and blocking first-time setup.

## What Was Built

**Task 1 -- SKILL.md (279 lines, under 500 line limit):**

`skills/editorial-translation/SKILL.md` -- the main skill file that Claude reads when triggered. Implements every locked decision from CONTEXT.md:

- **D-01/D-02:** Codex is opt-in via `--engine codex`. Claude translates by default. Falls back to Claude on any codex failure.
- **D-03:** When codex is selected, it handles both the analysis pass (read-only sandbox) and the translation pass (workspace-write sandbox).
- **D-04:** Codex model is `gpt-5.4-mini` at `medium` reasoning effort. Explicitly notes this overrides the codex skill's auto-selection table.
- **D-05/D-06:** No diagnostic phase, no Phase 1 issue log. Production-output skill: reads `.agents/editorial-context.md` but never writes to it.
- **D-07:** Single-mode workflow only. No `--mode` flag, no quick/normal/refined options in defaults table.
- **D-08/D-09:** Workflow Position section shows translation as an optional branch between stages 4 and 5, does not renumber existing stages.
- **D-10:** Default target language is `es` (Spanish) in defaults table, first-time setup prompt, and Usage section.
- **D-11:** Any language pair supported -- `--from` and `--to` flags accept any language code.
- **D-12:** Chunked translation triggered at 4000-word threshold. Configurable via `chunk_threshold` and `chunk_max_words` in EXTEND.md.
- **D-13:** Temp files for codex path go to `.tmp/analysis-prompt.md` and `.tmp/translate-prompt.md`. Cleanup after each run (success or failure).

Structure follows the plan spec exactly: 12 sections from frontmatter through Workflow Position.

## Decisions Made

1. **Production-skill pattern**: Modeled on `editorial-typesetting` -- intake, generate, output shape. No diagnostic phase, no issue log.

2. **Codex model version note**: The codex SKILL.md lists `gpt-5.2-mini`; CONTEXT.md D-04 specifies `gpt-5.4-mini`. SKILL.md uses `gpt-5.4-mini` and explicitly notes this overrides the auto-selection table, with advice to check current codex options if unavailable.

3. **Blocking first-time setup**: When EXTEND.md is not found, `AskUserQuestion` in ONE call collects all setup inputs (target language, source language, audience, style, save location). No silent defaults per Pitfall 5.

4. **stop-slop in Step 7**: Referenced during the translate step for production prose quality, consistent with CLAUDE.md convention for Phase 2 prose drafting.

5. **Translator's notes format**: Changed from baoyu-translate's Chinese format to general format: `[target text] ([source term] -- [brief explanation])`.

## Deviations from Plan

None. All acceptance criteria passed. The plan's automated verification check `grep "target_language.*es"` matched differently (table column order puts `es` before `target_language` in the row), but the content is present and correct at line 55: `| Target language | \`es\` | \`target_language\` |`.

## Known Stubs

None. SKILL.md is an instruction file for an AI agent, not a UI component with data wiring.

## Self-Check: PASSED

### Files exist:
- FOUND: skills/editorial-translation/SKILL.md

### Commits verified:
- FOUND: d7db275 feat(999.1-02): write SKILL.md for editorial-translation skill
